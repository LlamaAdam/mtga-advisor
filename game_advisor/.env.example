# See config.py for the full list of LLM_BACKEND options (ollama, openai,
# openrouter, claude). LLM_BACKEND=claude uses the subscription `claude`
# CLI instead — no OPENAI_API_KEY needed for that backend.
# LLM_BACKEND=claude

OPENAI_API_KEY=sk-or-v1-your-openrouter-key-here
ADVISOR_MONITOR_X=1920
ADVISOR_MONITOR_Y=0
