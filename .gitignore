# Secrets
.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp

# Caches that get rewritten frequently — never commit these.
# (Existing tracked copies need `git rm --cached` once; see DATA_CLEANUP.md.)
arena_id_cache.json
ratings_cache.json

# User state (per-installation, not code).
game_advisor/saved_decks.json

# Runtime logs (populated at execution time).
game_advisor/logs/
*.log

# Debug screenshots / captures.
Screenshot *.png
*.screenshot.png

# Claude Code per-user files.
.claude/scheduled_tasks.lock
.claude/settings.local.json
